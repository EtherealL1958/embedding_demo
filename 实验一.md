# 实验一

本实验使用了助教提供的框架和数据集，目标是训练并评估一个基于 BERT 的 dense retriever。实验包含四组对比：原始 BERT-base、Stage 1 LoRA 训练后的 BERT、Stage 2 hard negatives 继续训练后的 BERT，以及成熟 embedding 模型 BGE-base-en-v1.5。



## 实验架构说明

本项目是一个 Bert Dense retriever 训练与评估框架，主要用于比较原始 BERT、LoRA 微调 BERT、hard negative 继续训练，以及 BGE embedding 模型的检索排序效果。

项目架构如下：

- 一个基于 BERT 模型的 Bi-encoder Dense Retriever，即：将文章和用户query分别通过BERT进行embedding，然后比较其相似度。

- 使用DPR-Styled contrast Learning，即：构造正（高相关性）、负（低相关性）文章样本，让模型学习提高前者相似度，降低后者相似度
- 两阶段微调：第一阶段使用negative样本（侧重区分词句“表面”相似度），第二阶段使用hard-negative样本（侧重区分“内在”语义与问题相关程度）





## 参数配置



### 数据使用情况

| 项目                    | 设置 |
| ----------------------- | :--- |
| 训练集名称              | Stage 1: `data/dpr/biencoder-nq-train.json.gz`; Stage 2: `data/dpr/biencoder-nq-adv-hn-train.json.gz` |
| 训练样本数              | Stage 1: 50000; Stage 2: 50000 |
| 是否使用 hard negatives | Stage 1: 否; Stage 2: 是 |
| hard negative 样本数    | 50000 |
| 测试集名称              | `data/dpr/biencoder-nq-dev.json.gz` |
| 测试 query 数量         | 3000 |

训练时没有使用完整训练集，而是通过 `training_args.json`里的`MAX_TRAIN_SAMPLES` 使用每个训练文件的前 50000 条样本。测试时使用 `MAX_EVAL_SAMPLES=3000`，即 dev 集中的 3000 个 query。所有对比方法都使用相同的 dev query 和相同的候选集合；评估候选集合由每条样本中的 `positive_ctxs + negative_ctxs + hard_negative_ctxs` 构成，因此这里的指标是同候选集排序效果，而不是全库检索效果。

### 模型参数

| 参数                  | 设置 |
| --------------------- | ---- |
| backbone              | `models/bert-base-uncased` |
| pooling 方法          | mean pooling |
| embedding 维度        | 768 |
| 是否 L2 normalization | 是 |
| 相似度计算方式        | dot product；由于向量已归一化，等价于 cosine similarity |
| max query length      | 64 |
| max passage length    | 256 |
| passage 输入格式      | `title + "\n" + text`；如果没有 title，则只使用 text |

本实验采用 bi-encoder 结构，query 和 passage 分别编码，但共享同一个 BERT encoder。BERT 输出后使用 attention mask 排除 padding token，对有效 token 的 hidden states 做 mean pooling，再进行 L2 normalization。训练和评估时均使用 query embedding 与 passage embedding 的归一化点积作为排序分数。

### LoRA 参数

| 参数           | 设置 |
| -------------- | ---- |
| LoRA rank      | 16 |
| LoRA alpha     | 32 |
| LoRA dropout   | 0.05 |
| target modules | `query,value` |
| 可训练参数量   | 589,824 / 110,072,064，约 0.5359% |

LoRA adapter 加在 BERT attention 层的 `query` 和 `value` 线性模块上。训练时只更新 LoRA 参数，BERT 主体参数保持冻结，这种方法可以显著减少可训练参数量和显存开销，适合作为轻量的 fine-tuning 方法。

### 训练参数

| 参数                        | 设置 |
| --------------------------- | ---- |
| epoch                       | 1 |
| batch size                  | 32 |
| gradient accumulation       | 1 |
| learning rate               | Stage 1: 2e-5; Stage 2: 5e-6 |
| warmup steps / warmup ratio | warmup ratio = 0.06 |
| weight decay                | 0.01 |
| optimizer                   | AdamW |
| scheduler                   | linear scheduler with warmup |
| temperature                 | 0.05 |
| precision                   | fp16 |
| random seed                 | 42 |
| GPU                         | NVIDIA GeForce RTX 4090 |

训练损失为 Multiple Negatives Ranking Loss / InfoNCE。每个 batch 中的 passage 矩阵由 positive passages 和 negative passages 拼接得到，第 `i` 个 query 的正确 passage 是第 `i` 个 positive passage。logits 由 query 与 passages 的点积除以 temperature 得到，然后用 cross entropy 训练。

两阶段训练参数如下：

| 阶段    | 训练数据       | epoch | learning rate | batch size | warmup | 说明 |
| ------- | -------------- | ----: | ------------: | ---------: | -----: | ---- |
| Stage 1 | 普通负例       | 1 | 2e-5 | 32 | 0.06 | 使用 `negative_ctxs` 进行基础 retriever 训练 |
| Stage 2 | hard negatives | 1 | 5e-6 | 32 | 0.06 | 从 Stage 1 adapter 继续训练，使用 `hard_negative_ctxs`，学习率更小以避免破坏第一阶段已学到的表示 |

Stage 1 学习率较大，主要用于让原始 BERT 适应检索任务；Stage 2 在 hard negatives 上继续训练，负例更难、训练信号更细，因此使用更小的学习率进行微调。



## 实验结果与分析

以下实验均在2张 NVIDIA GeForce RTX 4090 上运行（所有实验记录见 `run_all.log`）

### 主结果表

**50000 sample + 1 epoch**

| 方法                              | MRR@10 | Recall@10 | Recall@20 | Recall@30 |
| --------------------------------- | -----: | --------: | --------: | --------: |
| BERT-base + mean pooling          | 0.2949 | 0.2459 | 0.4028 | 0.5230 |
| BERT-base + LoRA                  | 0.4754 | 0.4238 | 0.5715 | 0.6751 |
| BERT-base + LoRA + hard negatives | 0.4923 | 0.4370 | 0.5880 | 0.6896 |
| BGE-base-en-v1.5                  | 0.7500 | 0.6616 | 0.7851 | 0.8453 |

从结果看，LoRA 训练显著超过原始 BERT。MRR@10 从 0.2949 提升到 0.4754，Recall@10 从 0.2459 提升到 0.4238，说明仅使用原始 BERT 做 mean pooling 并不能很好适配检索排序任务，而对比学习训练可以明显改善 query-passage 匹配能力。

加入 hard negatives 后，Stage 2 相比 Stage 1 继续提升，但提升幅度较小。MRR@10 从 0.4754 提升到 0.4923，Recall@10 从 0.4238 提升到 0.4370。这说明 hard negatives 能进一步帮助模型区分语义接近但不正确的 passage，不过第二阶段只训练 1 个 epoch，且学习率较小，因此改进幅度较小。

自己训练的 BERT retriever 与 BGE-base-en-v1.5 仍有明显差距。BGE 的 MRR@10 为 0.7500，比 Stage 2 高 0.2577；Recall@10 为 0.6616，比 Stage 2 高 0.2247。这说明成熟 embedding 模型经过更大规模、更充分的预训练和检索对齐，在同候选集排序上仍明显优于本实验中轻量训练的 BERT + LoRA。

提升同时体现在 MRR@10 和 Recall@k 上。Stage 1 相对原始 BERT 的 MRR@10 提升较大，说明正确 passage 的排名显著提前；Recall@20、Recall@30 也同步提升，说明模型不仅把正确答案排得更靠前，也提高了在较大 top-k 范围内命中的概率。Stage 2 的增益更偏小幅整体提升，符合 hard negative fine-tuning 的作用：让模型在相似负例中做更细粒度区分。

## 改进

### 1. 增加训练规模

最简单的方法是增加样本量，跑多个epoch。下面是将样本量增加到了 100000，epoch改为 2 后的数据（两个阶段均做修改）。

**100000 sample + 2 epoch (all stages)**

| 方法                              | MRR@10 | Recall@10 | Recall@20 | Recall@30 |
| --------------------------------- | -----: | --------: | --------: | --------: |
| BERT-base + mean pooling          | 0.2949 | 0.2459 | 0.4028 | 0.5230 |
| BERT-base + LoRA                  | 0.4984 | 0.4487 | 0.6044 | 0.6984 |
| BERT-base + LoRA + hard negatives | 0.5352 | 0.4789 | 0.6320 | 0.7219 |
| BGE-base-en-v1.5                  | 0.7500 | 0.6616 | 0.7851 | 0.8453 |

在stage1，平均提升约2个百分点。stage2效果更好，提升接近4个百分点。效果相当明显。测试增加样本后，train+eval 总时间来到70min，在算力资源足够的情况下可以考虑。



### 2. 增加可训练参数比例

当前仅在 `query` 和 `value` 模块上加了 LoRA，可训练参数占比仅为 0.53%。我们可以人为地增加 Lora 中可训练参数比例，从而获得更为丰富的表示。由于对应命令行参数未提供，我们将 `train_stage1.sh` 和 `train_stage2` 中的 `lora_r` 改为 32， `lora_alpha` 改为 64。其他设置和改进1一致，可训练参数来到了 1.07%。以下为结果：

**100000 sample + 2 epoch (all stages) + lora_r=32 + lora_alpha=64**

| 方法                              | MRR@10 | Recall@10 | Recall@20 | Recall@30 |
| --------------------------------- | -----: | --------: | --------: | --------: |
| BERT-base + mean pooling          | 0.2949 | 0.2459 | 0.4028 | 0.5230 |
| BERT-base + LoRA                  | 0.5071 | 0.4567 | 0.6109 | 0.7046 |
| BERT-base + LoRA + hard negatives | 0.5518 | 0.4929 | 0.6440 | 0.7308 |
| BGE-base-en-v1.5                  | 0.7500 | 0.6616 | 0.7851 | 0.8453 |

平均提升约为1个百分点，验证了此方法的有效性。



### 3. 将Lora应用于更多层

当前实验仅在 attention 的 `query` 与 `value` 模块上加入 LoRA adapter，这是一种较为保守、参数量较小的配置。虽然这种方式已经能够以极低参数量获得明显收益，但其表达能力仍受到限制。进一步的改进方向是：将 LoRA 扩展到更多线性层，例如：

- `query + key + value`
- `query + value + output dense`

由于框架未提供相应的命令行参数，这里未做相应实验，只是提供一个可能的优化思路。