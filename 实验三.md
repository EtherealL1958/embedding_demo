# 实验三：Generator 微调与端到端 RAG 效果评估

## 1. 实验目标

实验三在实验二已经完成的完整 RAG 系统基础上，继续微调 generator，并比较微调前后的生成效果。

本实验沿用实验二选出的最优设置：

- Retriever：实验一训练得到的 `BERT-base + LoRA + hard negatives`，即 `models/bert-base-uncased` + `outputs/stage2`
- Reranker：`models/bge-reranker-base`
- Generator base：`models/Qwen2.5-1.5B-Instruct`
- Top-N：`30`
- 最优 k：`5`

实验重点验证两个问题：

- 在给定 gold passage 的条件下，LoRA 微调是否提升 generator 的读文回答能力。
- 在完整 RAG 条件下，LoRA 微调后的 generator 是否能进一步提升端到端回答质量。

## 2. 数据与训练样本构造

训练集使用 `data/clapnq/question_train_answerable.tsv`。按照作业要求，训练输入中的 passage 不直接使用 train 文件里的 gold passage，而是通过实验二构建好的 CLAPNQ RAG 数据库检索获得：

```text
question
  -> BERT LoRA retriever 检索 Top-30
  -> BGE reranker 重排
  -> 取 Top-5 passages
  -> 拼接成 RAG prompt
  -> 监督微调 Qwen 输出 reference long answer
```

训练时只对 assistant answer 部分计算 loss，question 和 passages 部分被 mask 掉，避免模型学习复述输入上下文。

## 3. 微调配置

| 项目 | 设置 |
|---|---|
| Base generator | `models/Qwen2.5-1.5B-Instruct` |
| 微调方式 | LoRA SFT |
| LoRA r | 16 |
| LoRA alpha | 32 |
| LoRA dropout | 0.05 |
| epoch | 2 |
| learning rate | 2e-5 |
| precision | bf16 |
| max input length | 1536 |
| max output length | 256 |
| batch size | 1 |
| gradient accumulation | 8 |
| adapter 输出目录 | `outputs/generator_lora` |

训练日志显示最终 `train_loss = 0.9680`，可训练参数约 `18.46M`，占总参数量 `1.18%`。

## 4. 对比实验设置

在 CLAPNQ dev 上评估，固定 `k = 5`。对比四组方法：

| 方法 | Retriever | Reranker | Generator | 输入 passage |
|---|---|---|---|---|
| 原始 Generator，无 RAG，gold passage | 无 | 无 | 原始 Qwen | dev gold passage |
| 微调 Generator，无 RAG，gold passage | 无 | 无 | Qwen + LoRA | dev gold passage |
| 原始 Generator，RAG with Reranker | BERT LoRA | BGE reranker | 原始 Qwen | 检索重排 Top-5 |
| 微调 Generator，RAG with Reranker | BERT LoRA | BGE reranker | Qwen + LoRA | 检索重排 Top-5 |

gold passage 只用于测试 generator 在理想证据条件下的读文能力；完整 RAG 评估不引入 gold passage。

## 5. 实验结果

| 方法 | k | EM | F1 | ROUGE-L |
|---|---:|---:|---:|---:|
| 原始 Generator，无 RAG，gold passage | - | 0.0000 | 0.3644 | 0.3326 |
| 微调 Generator，无 RAG，gold passage | - | 0.0133 | 0.4991 | 0.4848 |
| 原始 Generator，RAG with Reranker | 5 | 0.0000 | 0.3142 | 0.2829 |
| 微调 Generator，RAG with Reranker | 5 | 0.0100 | 0.4013 | 0.3752 |

微调后，在 gold passage 条件下 F1 提升 `0.1347`，ROUGE-L 提升 `0.1522`。这说明 LoRA SFT 提升了 generator 在给定证据文本时组织长答案的能力。

在完整 RAG 条件下，F1 从 `0.3142` 提升到 `0.4013`，ROUGE-L 从 `0.2829` 提升到 `0.3752`。这说明 generator 微调不仅改善理想 evidence 条件下的读文能力，也能改善真实检索 + reranker 场景下的端到端 RAG 效果。

## 6. 样例分析

### 6.1 明显提升样例

Question:

```text
who won the civil war the south or the north
```

Reference:

```text
The Union, the North, finally won the war when General Robert E. Lee surrendered to General Ulysses S. Grant at the Battle of Appomattox Court House, followed by a series of surrenders by Confederate generals throughout the southern states.
```

原始 Generator 只回答：

```text
The North won the Civil War.
```

微调后回答：

```text
The Union eventually won the war when General Robert E. Lee surrendered to General Ulysses S. Grant at the Battle of Appomattox Court House, followed by a series of surrenders by Confederate generals throughout the southern states.
```

F1 从 `0.1622` 提升到 `0.9538`。该样例说明微调后的 generator 更倾向于根据 passage 生成完整长答案，而不是只输出一个简短事实。

### 6.2 退化样例

Question:

```text
who sings the song story of an artist
```

Reference:

```text
Daniel Johnston sings ''The story of an artist''
```

原始 Generator 能正确回答 Daniel Johnston，但微调后错误生成了 Taylor Swift 的 `Love Story`。该样例说明微调并不能解决所有问题，在检索证据或相似标题干扰较强时，generator 仍可能被噪声 passage 误导。

### 6.3 仍然失败样例

Question:

```text
who is the longest running character on general hospital
```

原始模型和微调模型都生成了大量无意义年份序列，F1 均为 `0.0`。这说明端到端 RAG 仍存在 generator 稳定性问题，尤其是在上下文包含列表、日期或演员表等结构化噪声时，模型可能进入异常续写模式。

## 7. 结论

实验三表明，使用 CLAPNQ train 对 Qwen2.5-1.5B-Instruct 进行 LoRA 微调，可以明显提升 generator 利用 passages 生成长答案的能力。

在 gold passage 设置下，提升主要来自 generator 对证据文本的阅读和答案组织能力增强；在完整 RAG 设置下，提升说明微调后的 generator 能更好利用 retriever + reranker 提供的 Top-5 passages。

但失败样例也说明，RAG 系统最终效果仍同时受检索召回、reranker 排序、passage 噪声和 generator 解码稳定性影响。优化手段包括改进 prompt 约束、过滤低置信 passage、调小生成长度，或在训练时加入更多 hard/noisy retrieved contexts等。

## 8. 说明

- 微调代码：`src/exp3_generator/run_experiment.py`
- 运行脚本：`scripts/run_exp3.sh`
- LoRA adapter：`outputs/generator_lora`
- 结果表：`results/exp3/table_generator_finetune_comparison.json`
- 预测文件：`results/exp3/predictions_*.jsonl`
- 自动生成报告：`results/exp3/experiment3_report.md`
