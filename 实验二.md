# 实验二：完整 RAG 系统构建与评估

## 1. 实验目标

本实验使用实验一训练得到的 BERT dense retriever，在 CLAPNQ corpus 上构建完整 RAG 系统，并评估不同检索设置对最终问答效果的影响。

实验重点包括：

1. 使用 CLAPNQ passages 构建 FAISS 向量检索库；
2. 比较不同 `k` 值下 RAG + reranker 的检索效果和生成效果；
3. 固定最佳 `k*` 后，对比 No RAG、Random-k、RAG without reranker、RAG with reranker；
4. 分析 reranker 是否能改善证据排序，并进一步提升最终生成质量；
5. 对错误样例进行分析。

完整流程为：

```text
CLAPNQ dev question
  -> BERT retriever 在 CLAPNQ corpus 中检索 Top-N passages
  -> BGE reranker 对 Top-N passages 重新排序
  -> 取 Top-k passages 输入 Qwen generator
  -> 与 reference long answer 计算 F1 和 ROUGE-L
```

## 2. 实验设置

### 2.1 数据集

| 数据 | 路径 | 用途 | 数量 |
|---|---|---|---:|
| CLAPNQ passages corpus | `data/clapnq/passages.tsv` | RAG 检索语料库 | 178890 passages |
| CLAPNQ dev answerable | `data/clapnq/question_dev_answerable.tsv` | 完整 RAG 评估集 | 300 questions |

说明：`question_dev_answerable.tsv` 中部分 long answer 内部带换行，因此直接 `wc -l` 会大于真实样本数；实际按 TSV 解析得到 300 条样本。

### 2.2 模型与组件

| 组件 | 使用模型 / 方法 | 路径 |
|---|---|---|
| Retriever | 实验一 Stage 2 BERT + LoRA dense retriever | `models/bert-base-uncased` + `outputs/stage2` |
| Vector index | FAISS `IndexFlatIP` | `indexes/clapnq_stage2.faiss` |
| Reranker | BGE cross-encoder reranker | `models/bge-reranker-base` |
| Generator | Qwen2.5-1.5B-Instruct | `models/Qwen2.5-1.5B-Instruct` |

Retriever 使用 mean pooling 和 L2 normalization。由于向量已经归一化，FAISS 的 inner product 排序等价于 cosine similarity 排序。

### 2.3 关键参数

| 参数 | 设置 |
|---|---:|
| Top-N | 30 |
| k values | 1, 3, 5, 10 |
| query max length | 64 |
| passage max length | 256 |
| reranker max length | 512 |
| generator max input length | 1536 |
| max new tokens | 128 |
| temperature | 0.0 |
| random seed | 42 |

### 2.4 运行命令

准备数据：

```bash
bash scripts/prepare_exp2_data.sh
```

下载模型：

```bash
bash scripts/download_exp2_models.sh
```

构建 CLAPNQ FAISS 索引：

```bash
bash scripts/build_exp2_index.sh
```

运行完整实验二：

```bash
MAX_EVAL_SAMPLES=0 bash scripts/run_exp2.sh
```

实验输出位于：

```text
results/exp2/
```

主要结果文件包括：

| 文件 | 说明 |
|---|---|
| `retrieval_top30.jsonl` | BERT retriever 初始 Top-30 检索结果 |
| `reranked_top30.jsonl` | BGE reranker 重排后的 Top-30 结果 |
| `table1_k_generation.json` | 不同 k 的生成指标 |
| `table2_k_retrieval.json` | 不同 k 的检索指标 |
| `table3_comparison_generation.json` | 固定 k* 后四组方法生成指标 |
| `table4_comparison_retrieval.json` | 固定 k* 后 RAG 方法检索指标 |
| `predictions_*.jsonl` | 各实验组逐样本生成结果 |

## 3. 实现说明

实验二新增代码位于 `src/exp2_rag/`：

| 文件 | 作用 |
|---|---|
| `data.py` | 读取 CLAPNQ passages、questions、gold passage id 和 reference answers |
| `build_index.py` | 使用实验一 Stage 2 retriever 编码 corpus，并构建 FAISS index |
| `run_experiments.py` | 统一执行 retrieval、rerank、generation 和 metric aggregation |
| `metrics.py` | 实现 Recall@k、MRR@10、nDCG@10、token F1、ROUGE-L |
| `prompts.py` | 实现 No RAG 和 RAG prompt 模板 |

RAG prompt 使用作业要求的格式：

```text
You are given a question and several passages.
Answer the question using only the information in the passages.
If the passages do not contain enough information, answer "I don't know".

Question:
{question}

Passages:
[1] {title_1}: {passage_1}
...

Answer:
```

No RAG prompt 只输入 question，不提供 passages。

## 4. 评价指标

### 4.1 检索指标

| 指标 | 含义 |
|---|---|
| Recall@k | Top-k 中是否包含 gold passage；CLAPNQ 每个 answerable question 通常对应一个 gold passage |
| MRR@10 | gold passage 在 Top-10 中排名越靠前，得分越高 |
| nDCG@10 | 考虑 gold passage 排名位置的排序指标 |

### 4.2 生成指标

| 指标 | 含义 |
|---|---|
| F1 | 生成答案与 reference answer 的 token overlap F1 |
| ROUGE-L | 生成答案与 reference answer 的最长公共子序列 F1 |

CLAPNQ 是 long-form answer 数据集，EM 通常很低，因此本实验按作业要求重点报告 F1 和 ROUGE-L。

## 5. 不同 k 的影响实验

本部分固定完整 RAG 设置：

```text
BERT retriever + BGE reranker + Qwen generator
```

先由 retriever 召回 Top-30，再由 reranker 重排，并分别取 `k = 1, 3, 5, 10` 个 passages 输入 generator。

### 表 1：RAG + Reranker 下不同 k 的生成效果

| k | F1 | ROUGE-L |
|---:|---:|---:|
| 1 | 0.3086 | 0.2849 |
| 3 | 0.3133 | 0.2855 |
| 5 | 0.3187 | 0.2862 |
| 10 | 0.2289 | 0.2090 |

### 表 2：RAG + Reranker 下不同 k 的检索效果

| k | Recall@k | MRR@10 | nDCG@10 |
|---:|---:|---:|---:|
| 1 | 0.4533 | 0.4533 | 0.4533 |
| 3 | 0.6633 | 0.5461 | 0.5762 |
| 5 | 0.7600 | 0.5683 | 0.6161 |
| 10 | 0.8200 | 0.5765 | 0.6357 |

### k 的影响分析

从检索指标看，随着 `k` 增大，Recall@k 持续提升：从 `k=1` 的 0.4533 提升到 `k=10` 的 0.8200。这说明更多 passages 确实提高了 gold evidence 被输入 generator 的概率。

但是生成指标不是单调提升。`k=1 -> 3 -> 5` 时 F1 和 ROUGE-L 小幅上升，说明适量增加 passage 可以补充证据；当 `k=10` 时，F1 从 0.3187 明显下降到 0.2289，ROUGE-L 从 0.2862 下降到 0.2090。原因主要有两点：

1. `k=10` 引入了更多与问题表面相关但不一定支持答案的噪声 passage；
2. generator 输入长度有限，passages 太多时有效证据可能被噪声稀释，模型更容易生成无关片段或输出不完整答案。

因此本实验选择：

```text
k* = 5
```

`k=5` 在 CLAPNQ dev 上取得最高 F1 和最高 ROUGE-L，同时检索 Recall@5 已达到 0.7600，是生成质量和输入噪声之间较好的折中。

## 6. 固定 k* 后的四组对比实验

固定：

```text
Top-N = 30
k* = 5
```

比较四组方法：

| 方法 | 输入给 Generator 的内容 |
|---|---|
| No RAG | 只输入 question |
| Random-k | question + 随机抽取的 5 个 passages |
| RAG Top-k without Reranker | question + BERT retriever 直接检索的 Top-5 passages |
| RAG Top-k with Reranker | question + BERT retriever Top-30 经 BGE reranker 重排后的 Top-5 passages |

### 表 3：固定 k* 后四组方法的生成效果

| 方法 | k | F1 | ROUGE-L |
|---|---:|---:|---:|
| No RAG | - | 0.1760 | 0.1429 |
| Random-k | 5 | 0.1583 | 0.1608 |
| RAG Top-k without Reranker | 5 | 0.2978 | 0.2646 |
| RAG Top-k with Reranker | 5 | 0.3187 | 0.2862 |

### 表 4：固定 k* 后 RAG 方法的检索效果

| 方法 | k | Recall@k | MRR@10 | nDCG@10 |
|---|---:|---:|---:|---:|
| RAG Top-k without Reranker | 5 | 0.5600 | 0.3280 | 0.3851 |
| RAG Top-k with Reranker | 5 | 0.7600 | 0.5683 | 0.6161 |

## 7. 对比结果分析

### 7.1 RAG 是否优于 No RAG

完整 RAG with reranker 的 F1 为 0.3187，高于 No RAG 的 0.1760，绝对提升 0.1427，相对提升约 81.1%。ROUGE-L 从 0.1429 提升到 0.2862，绝对提升 0.1433。

这说明 CLAPNQ 中很多问题依赖外部知识，仅靠 Qwen 自身参数知识时，模型经常输出 `I don't know` 或只给出泛化答案；加入检索证据后，generator 可以直接从 passage 中抽取或组织答案，因此端到端问答质量明显提升。

### 7.2 RAG 是否优于 Random-k

Random-k 的 F1 为 0.1583，低于 No RAG，也明显低于完整 RAG 的 0.3187。完整 RAG 相对 Random-k 的 F1 绝对提升 0.1604，相对提升约 101.4%。

这说明“输入更多文本”本身不会自然带来提升。只有当输入 passages 与问题相关，并包含可支持答案的证据时，generator 才能稳定受益。随机 passage 反而可能引入噪声，使模型更难判断哪些内容可用。

### 7.3 Reranker 是否有贡献

在生成指标上，RAG with reranker 的 F1 为 0.3187，高于 without reranker 的 0.2978；ROUGE-L 从 0.2646 提升到 0.2862。提升幅度不如“有无 RAG”那么大，但仍然稳定为正。

在检索指标上，reranker 的贡献更明显：

| 指标 | without reranker | with reranker | 绝对提升 |
|---|---:|---:|---:|
| Recall@5 | 0.5600 | 0.7600 | +0.2000 |
| MRR@10 | 0.3280 | 0.5683 | +0.2403 |
| nDCG@10 | 0.3851 | 0.6161 | +0.2310 |

BERT retriever 的 Top-30 Recall 为 0.8433，说明第一阶段已经能召回大量 gold passages；reranker 并不改变 Top-30 候选集合，而是把更相关的 passage 往前排。因此它对 MRR@10 和 nDCG@10 的提升尤其明显，也解释了为什么最终生成效果进一步改善。

## 8. 错误样例分析

### 8.1 成功样例：reranker 将 gold passage 从第 15 位提升到第 1 位

| 项目 | 内容 |
|---|---|
| question | the latin phrase parens patriae was used to refer to |
| gold passage rank before rerank | 15 |
| gold passage rank after rerank | 1 |
| RAG with reranker prediction | The Latin phrase "parens patriae" was used to refer to the public policy power of the state to intervene against an abusive or negligent parent, legal guardian, or informal caretaker, and to act as the parent of any child or individual who is in need of protection. |
| reference answer | In law, Latin Parens patriae refers to the public policy power of the state to intervene against an abusive or negligent parent, legal guardian, or informal caretaker, and to act as the parent of any child or individual who is in need of protection. |
| score | F1 = 0.9024, ROUGE-L = 0.9231 |

这个例子体现了 reranker 的核心作用：初始 retriever 已经召回了 gold passage，但排在第 15 位，若直接取 Top-5 则无法输入 generator；经过 reranker 后，gold passage 被排到第 1 位，最终生成几乎完整覆盖 reference answer。

### 8.2 失败样例：第一阶段 Top-30 没有召回 gold passage

| 项目 | 内容 |
|---|---|
| question | band that got in trouble for lip syncing |
| gold passage id | `800075295_2911-4116` |
| gold passage rank in retriever Top-30 | 未召回 |
| RAG with reranker prediction | The passage does not provide any specific information about a band getting into trouble for lip-syncing. Therefore, my answer is: I don't know. |
| reference answer | The first public sign that Milli Vanilli was lip-synching came on 21 July 1989, during a live performance on MTV at the Lake Compounce theme park in Bristol, Connecticut. |
| score | F1 = 0.0000, ROUGE-L = 0.0727 |

这个例子说明 RAG 系统的上限仍受第一阶段 retriever 召回限制。如果 gold passage 不在 Top-30 中，reranker 无法重新排序到正确证据，generator 也只能基于错误或无关 passage 回答 `I don't know`。

### 8.3 失败样例：gold passage 已排第 1，但 generator 生成异常

| 项目 | 内容 |
|---|---|
| question | who is the longest running character on general hospital |
| gold passage rank after rerank | 1 |
| RAG with reranker prediction | 2017, 2018 -- 2019, 2020, 2021, 2022, ... |
| reference answer | The longest running character in General Hospital is Dr. Monica Quartermaine who has been portrayed by Leslie Charleson since August 17, 1977. |
| score | F1 = 0.0000, ROUGE-L = 0.0000 |

这个例子中检索和重排已经成功，但 generator 输出了无意义的年份序列，说明端到端 RAG 不只依赖检索质量，也依赖 generator 对上下文的读取和生成稳定性。后续可以通过更强的 prompt 约束、降低输出长度、或在实验三中微调 generator 来改善这类问题。

## 9. 总结

本实验完成了 CLAPNQ corpus 上的完整 RAG 系统构建与评估。主要结论如下：

1. 使用实验一训练好的 BERT LoRA retriever 可以在 CLAPNQ corpus 上完成有效的 dense retrieval，Top-30 Recall 达到 0.8433；
2. 增大 `k` 会持续提高 Recall@k，但生成效果并不单调提升，`k=10` 因噪声增加导致 F1 和 ROUGE-L 明显下降；
3. 最佳 `k*` 为 5，在生成指标上取得 F1 = 0.3187、ROUGE-L = 0.2862；
4. 完整 RAG 明显优于 No RAG 和 Random-k，说明外部检索证据对 CLAPNQ 问答非常重要；
5. BGE reranker 显著改善排序质量，Recall@5 从 0.5600 提升到 0.7600，MRR@10 从 0.3280 提升到 0.5683，并带来生成指标的进一步提升；
6. 错误主要来自两类：第一阶段未召回 gold passage，以及 generator 即使拿到正确证据仍生成异常或拒答。

整体来看，实验二验证了完整 RAG pipeline 的有效性，也说明 retriever recall、reranker 排序和 generator 上下文利用能力都会影响端到端问答效果。
